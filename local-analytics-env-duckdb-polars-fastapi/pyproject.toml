[project]
name = "local-analytics-env-duckdb-polars-fastapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Avinash", email = "avinash.yadav@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "duckdb>=1.5.5",
    "fastapi>=0.141.1",
    "numpy>=2.5.2",
    "polars>=1.43.2",
    "pyarrow>=25.0.1",
    "uvicorn>=0.52.1",
]

[project.scripts]
local-analytics-env-duckdb-polars-fastapi = "local_analytics_env_duckdb_polars_fastapi:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
